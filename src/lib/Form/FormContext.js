import React from 'react';

// TODO;  react context should be dynamic to initilization and every form must pass context  of that instance to its children Input cOmponent;

const FormContext = React.createContext('form');

export default FormContext;